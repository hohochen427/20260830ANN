# 影像生成 API 設定
#
# 用法：複製本檔為 .env，把金鑰填進 IMAGE_API_KEY。
#   cp .env.example .env
#
# .env 已被 .gitignore 排除，金鑰不會進版控。不要把金鑰填進本檔。

# ↓↓↓ 只有這一行需要你填 ↓↓↓
IMAGE_API_KEY=

# 供應商。預設 openai（ChatGPT 的 API key 就是填這個）
# 其他可選：google | stability | replicate
IMAGE_PROVIDER=openai

# 模型。留空使用 gpt-image-1
# 若你的帳號尚未完成組織驗證而拿到 403，改填 dall-e-3
IMAGE_MODEL=

# 長邊像素。實際尺寸依每張圖的長寬比自動選擇 OpenAI 支援的值
IMAGE_SIZE=1024
